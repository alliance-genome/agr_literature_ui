version: "3.7"

services:
  ui:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - "./nginx.conf:/etc/nginx/nginx.conf"
    ports:
      - "${LIT_UI_PORT}:80"
